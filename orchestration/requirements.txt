# Extra Python dependencies for Airflow DAG runtime (Airflow core comes from the base image).
apache-airflow-providers-apache-spark
attrs
authlib
cachetools
certifi
confluent-kafka
fastavro
httpx
minio
# Keep in sync with spark-iceberg image tag (Spark 3.5.1) to avoid driver/executor binary mismatches.
pyspark==3.5.1
requests
apache-airflow-providers-apache-kafka


scikit-learn
